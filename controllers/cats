const Cat = require('../models/cat')

exports.getIndex = async function (req, res, next) {
  const [cat] = await Cat.fetchRandomCat()
  if (!cat) res.redirect('/')
  res.render('index', {
    imgUrl: cat.url,
    path: undefined
  })
}

exports.getBreeds = async function (req, res, next) {
  const breeds = await Cat.fetchCatBreeds()
  res.render('breeds', {
    breeds: breeds,
    path: undefined
  })
}

exports.getBreed = async function (req, res, next) {
  console.log('getBreed func');
  console.log(req.params);
  const breedData = await Cat.fetchCatBreed()
  console.log(breedData);
  // res.render('breeds', {
  //   breeds: breeds,
  //   path: undefined
  // })
}
